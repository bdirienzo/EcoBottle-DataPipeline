# En este TP, "Load" = ya guardar a CSV en /warehouse. Aquí podrías
# agregar hooks para cargar a SQLite/Postgres/BigQuery si lo necesitás.
# Por ahora, no hace falta más que lo que ya hacen write_dim/write_fact.

def load():
    # Placeholder por si mañana querés mover CSV -> DB
    return True
